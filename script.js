function myGrades(score) {
  let sum = 0;
  for ( let i = 0; i < myGrades.length; i++) {
    sum = myGrades[i];
  }
  return score;
} 

function campsiteCount(camgrounds) {
  let counter = 0;
  for (let i = 0; i < campgrounds.length; i++) {
    counter = campgrounds.length;
  }
  return counter;
} 